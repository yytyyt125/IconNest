# Changelog

IconNest 的版本以单文件 `IconNest.exe` 的发布基线为准。本文记录对**用户可感知**的变更；开发期内部的六轮对抗式审查（坐标还原、冲突改名等）已随对应版本一并收敛，未单独列出。

格式参考 [Keep a Changelog](https://keepachangelog.com/)。

---

## [v0.2.12] — 2026-09-16（当前公开版）

图标清晰度与排序的最后一轮打磨。

### 修复
- **图标清晰度**：非快捷方式类型（文件夹 / Word / Excel / PPT / 图片等）改用 `IShellItemImageFactory` 原生管线提取，与桌面图标一致；修复图标周围**白色细边**（alpha 直通格式误用预乘导致）。
- **手动排序**：拖入图标后**不再自动排序**，排列顺序由用户手动调节；拖入位置按松手落点插入。
- **网格**：图标显示尺寸与桌面原始图标一致；列数随窗口宽度自适应（无横向滚动条）；行距收紧。

---

## [v0.2.7] ~ [v0.2.11] — 图标与网格迭代

- 图标提取源由 EXTRALARGE(48px) 升级为 **JUMBO(256px) 优先三级回退**，并在高 DPI 下按设备像素精确提取；`.lnk` 显式解析目标图标。
- 诊断日志实证法定位并修复 `nIconSize` 编码反写、`.lnk` 目标未解析等问题。
- 引入诊断日志 `show_debug.log` 用于图标分支追踪。

---

## [v0.2.0] ~ [v0.2.6] — 视觉质感迭代

- **v0.2.0** 实时透视背景：窗口背景实时反映桌面壁纸与图标变化（事件驱动失效，非每帧截屏）。
- **v0.2.1 ~ v0.2.6** 边缘融合 / 羽化边框：三级渐进模糊（raw→soft→full）、渐变层四边渐隐、移除系统描边、像素对齐消除发丝硬边、边缘保留主体色调、羽化开启时自动关阴影。

---

## [v0.1.3] — 2026-09-15（首个稳定基线）

核心功能全部落地并经真机验收。

### 新增
- **分类收纳**：桌面图标按分类拖入独立悬浮窗口；真实移动到桌面分类文件夹，拖出移回桌面。
- **多窗口**：一窗一分类，多窗口并存，窗口名自定义（改名联动存储目录）。
- **原生体验**：双击 = 系统原生打开；右键 = 完整系统原生菜单（含第三方注入项）+「移出分类（移回桌面）」。
- **外观**：6 套分类主题预设；外观面板（模糊度 0–40、透明度 30–95%、单色/渐变、取色器），逐窗独立记忆。
- **布局还原**：退出时（开关，默认开）把分类文件移回桌面并还原图标位置；关机/重启不还原；崩溃后下次启动**后台静默补偿**。
- **自启动**：启动文件夹快捷方式（无注册表、无 %APPDATA%），开机后按配置恢复。
- **分类管理**：删除分类 → 文件还原桌面原位置；点两下重命名（桌面同款语义）。
- **托盘**：暗色卡片右键菜单、设置页（存储位置迁移 / 退出还原开关 / 自启动）、清理并退出。
- **绿色性**：单文件 exe，配置存 exe 同级 `config\`，无安装器、无注册表写入。

---

## 版本号说明

- **v0.1.x**：功能开发 + 对抗式质量收敛阶段。
- **v0.2.x**：视觉质感（羽化边框）与图标清晰度 / 网格布局增强阶段。
- 发布基线文件：`src/IconNest/publish/IconNest.exe`。
